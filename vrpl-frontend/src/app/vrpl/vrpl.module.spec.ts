
import { VrplModule } from './vrpl.module';

describe('VrplModule', () => {
  let vrplModule: VrplModule;

  beforeEach(() => {
    vrplModule = new VrplModule();
  });

  /*  
  it('should create an instance', () => {
    expect(vrplModule).toBeTruthy();
  });
  */
  
});
