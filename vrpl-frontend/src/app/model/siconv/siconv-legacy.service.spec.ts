import { TestBed } from '@angular/core/testing';

import { SiconvLegacyService } from './siconv-legacy.service';

describe('SiconvLegacyService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  /*
  it('should be created', () => {
    const service: SiconvLegacyService = TestBed.get(SiconvLegacyService);
    expect(service).toBeTruthy();
  });
  */
});
